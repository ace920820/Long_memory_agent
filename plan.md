# 项目开发计划

## 当前进度

### 已完成功能
1. 向量库功能
   - 文档上传：支持文档内容上传到向量库
   - 文档检索：支持基于向量相似度的文档检索
   - 智能问答：基于检索结果进行上下文相关的问答

2. RAPTOR树形结构
   - 实现了基于RAPTOR的树形文档组织
   - 完成了RaptorModule的get_documents方法
   - 通过self.doc_storage.get_tree_info()可获取树结构信息
   - 实现增量更新和局部重构机制：
     - 每轮对话只增量插入新节点，不重构整个树
     - 根据配置的对话轮数触发树重构
     - 优化了内存使用和性能开销
     - 支持灵活的配置选项

3. 角色对话系统
   - 支持不同角色的对话风格切换
   - 支持情感智能响应
   - 实现稳定的角色个性管理

4. 记忆管理系统
   - 实现自适应的长短时记忆管理
   - 支持基于语义相似度的动态聚类
   - 建立记忆簇系统提升效率
   - 优化记忆检索机制：
     - 使用RetrievalAugmentation.retrieve()方法
     - 支持多层级的记忆检索
     - 优化了检索结果的排序和展示

5. 知识库管理
   - 完成文档的上传和解析功能
   - 更新RAPTOR的管理和解析方法
   - 具备基本的文档存储和检索能力

### tree_info数据结构示例
```json
{
    "tree": {
        "leaf_nodes": [
            {
                "text": "文档内容",
                "index": 0
            }
        ],
        "summary_nodes": [
            {
                "text": "摘要内容",
                "index": 87,
                "children": [67, 52, 54]
            }
        ],
        "num_layers": 1,
        "total_nodes": 92
    }
}
```

## 当前问题

### 1. 记忆管理模块问题
- [x] 缺少增量更新机制：已实现增量节点插入和定期重构
- [x] 检索效率问题：已通过树形结构优化检索效率
- [ ] 记忆优先级管理：需要基于创建时间和调用频次实现priority score
- [ ] 记忆内容优化：需要从对话内容生成摘要，而非直接存储原文
- [ ] 记忆节点管理优化：需要改进添加记忆节点的判断逻辑，不仅依赖相似度阈值

### 2. 知识库问答模块问题
- [x] 文档即时上传：已实现基于每次对话上传的文档进行检索
- [ ] 强行使用文档回答：对不相关问题也尝试从文档中寻找答案
- [ ] RAPTOR的embedding精度不足：语义匹配精度较低，多文档场景易出错
- [ ] 语义匹配增强：需要增加embedding模型和reranker模块

### 3. 界面问题
- [x] 不支持创建新对话和多对话切换
- [ ] 文件上传功能（文档、图片）不完善
- [ ] Markdown渲染支持不完善
- [ ] 缺少文档上传按钮和解析进度条

## 待开发功能

### 1. 前端页面开发
#### 1.1 文档树可视化界面
- [ ] 响应式布局设计
  - 使用CSS Grid和Flexbox实现自适应布局
  - 适配桌面端和移动端显示
  - 实现暗黑模式支持

- [ ] 树形结构交互
  - 使用HTML5语义化标签构建树形结构
  - 实现节点展开/折叠动画效果
  - 添加节点拖拽功能
  - 优化大规模数据的渲染性能

- [ ] 文档内容展示
  - 实现Markdown文本渲染
  - 支持代码语法高亮
  - 添加目录导航功能
  - 优化长文本加载性能

#### 1.2 文档管理界面
- [ ] 文件上传组件
  - 支持拖拽上传
  - 显示上传进度条
  - 文件类型校验
  - 大文件分片上传
  - 文档解析进度显示

- [ ] 文档列表
  - 支持分页和排序
  - 添加搜索和筛选功能
  - 实现列表项的批量操作
  - 优化列表滚动性能

#### 1.3 对话管理界面
- [x] 新建对话功能
  - 支持创建新对话
  - 角色选择功能
  - 独立的对话记忆和状态

- [x] 多对话切换
  - 多窗口切换支持
  - 历史对话查看
  - 对话状态保存

- [ ] 文件处理增强
  - 文档即时上传使用
  - 图片上传与OCR
  - 文件与对话关联

### 2. 后端功能增强
#### 2.1 记忆管理增强
- [x] 增量更新与局部重构优化
  - 已实现对话数据的"增量节点插入 + 定期重构"策略
    - 每轮对话只增量插入新节点，不重构整个树
    - 实现了可配置的定期重构机制
    - 优化了内存使用和性能
  - 已完成的优化方案：
    - 将最新记忆直接插入到树结构
    - 通过config.yaml配置重构参数
    - 支持灵活的重构策略
  - 已优化的查询策略：
    - 使用RetrievalAugmentation.retrieve()方法
    - 支持多层级检索
    - 优化了检索结果的排序

- [ ] 记忆优先级系统
  - 实现基于创建时间的优先级计算
  - 添加调用频次统计
  - 优化priority score计算公式
  - 将优先级应用于检索排序

- [ ] 摘要系统优化
  - 从对话内容生成高质量摘要
  - 优化摘要存储结构
  - 提升摘要生成效率
  - 实现增量摘要更新

- [ ] 记忆节点管理优化
  - 改进节点添加判断逻辑
  - 优化相似度计算方法
  - 添加更多判断维度
  - 实现智能合并策略

#### 2.2 知识库问答优化
- [ ] 语义匹配增强
  - 更换高效embedding模型
  - 添加reranker模块
  - 优化多文档场景

- [ ] 上下文理解
  - 意图识别模块
  - 问题相关度判断
  - 智能答案选择

- [ ] 文档处理优化
  - 即时上传处理
  - 动态树结构更新
  - 文档关联分析

### 3. 系统优化
- [ ] 前端性能优化
  - 实现组件懒加载（按需求加载文档RA树的不同层级）
  - 优化资源加载策略
  - 添加页面缓存机制
  - 实现虚拟滚动

- [ ] 后端性能优化
  - 实现树结构缓存
  - 优化检索算法
  - 添加异步处理支持
  - 实现分布式部署支持

## 开发优先级
1. 开发基础的文档树可视化界面，确保基本功能可用
2. 实现文档管理的核心功能
3. 完善RaptorModule功能，确保文档管理的完整性
4. 进行性能优化，提升用户体验

## 开发规范
1. 前端开发规范
   - 使用语义化HTML标签
   - 编写响应式CSS样式
   - 添加中文注释
   - 遵循W3C标准
   - 确保代码可维护性

2. 后端开发规范
   - 添加详细的中文注释
   - 实现完整的日志记录
   - 做好异常处理
   - 编写单元测试
   - 确保代码可扩展性

## 短期任务安排
- [x] 记忆管理模块
   - 引入RAPTOR的多层级结构
   - 实现记忆的层次化管理
   - 完善摘要生成机制

- [x] 知识库问答模块
   - 修改界面支持即时上传文档
   - 引入上下文相关度判断
   - 优化语义匹配精度

- [x] 界面开发
   - 实现新建对话功能
   - 支持多对话切换
   - 完善文件上传功能

## 前端开发详细规划

### 1. UI设计规范
- [ ] 设计系统建立
  - 定义颜色系统
    - 主色调
    - 辅助色
    - 状态色（成功、警告、错误）
  - 建立字体系统
    - 中文字体
    - 代码字体
    - 字号层级
  - 组件设计规范
    - 按钮样式
    - 输入框样式
    - 对话框样式

### 2. 页面布局优化
- [ ] 响应式布局增强
  - 移动端优先设计
  - 断点设置（手机、平板、桌面）
  - 弹性布局适配

### 3. 对话界面设计
- [ ] 聊天窗口优化
  - 气泡样式设计
  - 消息时间显示
  - 头像和角色标识
  - 输入框自适应

- [ ] 文件处理优化
  - 拖拽上传区域
  - 上传进度显示
  - 文件预览功能
  - 图片压缩处理

### 4. 文档树可视化
- [ ] 树形结构交互
  - 自定义树节点样式
  - 展开/折叠动画
  - 节点间连接线
  - 缩放和平移控制

- [ ] 内容展示优化
  - Markdown渲染美化
  - 代码高亮主题
  - 图表可视化
  - 长文本折叠
